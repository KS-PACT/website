<!doctype html>
<html>
  <head>
    <title>Hardware</title>
  </head>
  <body>
    <%- include header.ejs %>
		<% for(var i=0; i<data.length; i++) { %>
			<div class="w3-card-4">
				<!-- Need to add logic to replace the default image if actual image exists. <%= data[i].picture %> -->
				<div class="card-image">
					<img src="images/defaults/hardware_default.jpg" />
				</div>
				<div class="w3-container w3-left">
					<h3><%= data[i].name %></h3>
					<p><%= data[i].serial_num %></p>
				</div>
			</div>
    <% } %>
		<div class="w3-card-4">
			<div class="card-image">
				<img src="images/defaults/add_button.png" />
			</div>
		</div>
  </body>
</html>
